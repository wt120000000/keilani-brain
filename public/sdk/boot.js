window.Keilani && window.Keilani.createWidget({
  mount: "#keilani-widget",
  agent: "keilani",
  apiBase: "",                 // absolute to site root → /api/chat-stream
  theme: { brand: "#00ffcc" }
});